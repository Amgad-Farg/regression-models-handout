# Install everything with:   pip install -r requirements.txt
#
# These are minimum versions. The notebook was written and fully executed against
# the exact versions listed in the comment beside each line, so if something
# behaves oddly, pin to those.

pandas>=2.0            # tested with 3.0.2
numpy>=1.24            # tested with 2.4.4
scikit-learn>=1.4      # tested with 1.8.0
matplotlib>=3.7        # tested with 3.10.9
joblib>=1.3            # tested with 1.5.3
jupyter>=1.0           # to open and run the notebook
streamlit>=1.30         # to run streamlit_app.py (interactive price predictor)
